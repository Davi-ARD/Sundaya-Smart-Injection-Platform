# Dependensi
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Environment (jangan pernah commit .env asli)
.env
.env.*
!.env.example

# Coverage dan log
coverage/
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

.pnpm-store/
*.db

# File lock sementara Microsoft Office (mis. ~$dokumen.docx saat file dibuka)
~$*

# File upload user (avatar dll.) - tidak dilacak, disimpan lokal saja
apps/api/uploads/

# Konfigurasi tooling AI lokal per developer (tidak dibagikan ke repo tim).
.claude/